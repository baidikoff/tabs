// swift-tools-version:5.1

import PackageDescription

let package = Package(
  name: "Tabs",
  dependencies: [
        
  ],
  targets: [
    .target(
        name: "Tabs",
        path: "./Tabs/Sources"
        dependencies: [])
  ]
)
